Refinement brief

scope: auditor/graph
commit: 0f1e2d3c4b5a69788796a5b4c3d2e1f00f1e2d3c
targets: 4 of 9 queue rows under this scope
limits: 12 targets per run, 25 corrections per run

Targets

1. auditor/graph/refine/service.py::RefinementService.commit
   at auditor/graph/refine/service.py:42, unimportable_name: the callee 'load_user', called bare
   definers: auditor/graph/svc.py::load_user
   facts on commit: commit, load_user
   actions: add_edge, annotate_node, unresolvable

2. auditor/graph/refine/overlay.py::Overlay.apply
   at auditor/graph/refine/overlay.py:88, ambiguous_name: the attr_callee 'apply', called attr on
      overlay
   candidates: auditor/graph/a.py::apply, auditor/graph/b.py::apply
   definers: auditor/graph/a.py::apply, auditor/graph/b.py::apply
   resolution path: auditor/graph/refine/overlay.py
   actions: add_edge, resolve_ambiguous, annotate_node, unresolvable

3. auditor/graph/svc.py::Base
   at auditor/graph/svc.py:1, text_sparse: the node 'Base'
   actions: annotate_node, unresolvable

4. auditor/graph/svc.py::load_user
   at auditor/graph/svc.py:7, singleton_cluster: the cluster 'get' anchored here
   actions: annotate_node, unresolvable

Stale corrections

   7 add_edge stale: auditor/graph/a.py::main -> auditor/graph/b.py::read_event

Verdicts so far

   staged add_edge tier B: the call site and the definition both read

What to do

   actions available here: add_edge, annotate_node, resolve_ambiguous, unresolvable
   Read the source before every proposal, then call mcp__graph__propose once per target at most.
      Call mcp__graph__brief to re-read this with your verdicts.

   Answer with: summary (one line), proposed (how many you made), stopped_because (done | unsure |
      budget | nothing_to_do).
